apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-conf
data:
  default.conf: |
    server {
        listen 80;
        charset utf-8;
        location / {
            root /usr/share/nginx/html;
        }
        location /api/ {
            proxy_pass http://backend-svc:5001;
        }
}
